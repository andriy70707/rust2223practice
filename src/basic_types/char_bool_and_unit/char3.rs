#[test]
fn main() {
    let _f: bool = false;

    let t = false;
    if !t {
        println!("Success!");
        }
}

//в коді не виконується if !t бо воно в значенні true щоб воно виводило Success потрібно
//перевести змінну в false