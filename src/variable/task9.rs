#[test]
fn main() {
    let (x, y);
    (x, ..) = (3, 4);      // Деструктуризація кортежу, x = 3
    [.., y] = [1, 2];      // Деструктуризація масиву, y = 2

    // пропуск для правильного порівняння
    assert_eq!([x, y], [3, 2]);

    println!("Success!");
}
