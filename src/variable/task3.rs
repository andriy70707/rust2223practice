#[test]
fn main() {
    let x: i32 = 10; // x знаходиться в області видимості

    {
        let y: i32 = 5; // y в області видимості тільки в цьому блоці
        println!("x: {}, y: {}", x, y); // Коректно, обидві змінні x і y доступні
    }

    println!("x: {}", x); // Коректно, x досі в області видимості
    // println!("y: {}", y); // Помилка, y поза межами області видимості
}
