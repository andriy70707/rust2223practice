#[test]
fn main() {
    let x: i32 = 5;
    {
        let x = 12;
        assert_eq!(x, 12); // У внутрішньому блоці `x` дорівнює 12
    }

    assert_eq!(x, 5); // Зовнішня змінна `x` досі дорівнює 5

    let x = 42;
    println!("{}", x); // Виводить "42"
}
