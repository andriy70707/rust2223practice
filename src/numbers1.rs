#[test]
fn main() {
    let x: i32 = 5;
    let mut y: u32 = 5;

    // y = x;  // Це треба видалити, оскільки типи не збігаються

    let z = 10; // Тип z буде i32 за замовчуванням

    println!("Success!");
}
